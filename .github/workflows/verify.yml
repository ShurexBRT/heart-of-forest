name: Verify Golden Slice

on:
  push:
    branches:
      - main
      - polish/golden-slice
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Run syntax, static smoke and tests
        run: node scripts/verify.mjs
